# CSVヘッダーを出力
(["ServicePermissionId", "Principal"] | @csv),
(
  # 変更点：一番外側の配列を展開するため、先頭に .[] を追加
  .[].AllowedPrincipals[] |
  {
    ServicePermissionId: .ServicePermissionId,
    Principal: .Principal
  } |
  [.ServicePermissionId, .Principal] | @csv
)

# PowerShellのコマンドを使わず、単純な文字列連結のみでパスを作成します
$jqQueryDir = "C:\Users\123456\scripts\jq_query"
$subDirName = "AAA"
$baseName   = "filename"

$finalPath = $jqQueryDir + "\" + $subDirName + "\" + $baseName + ".jq"

Write-Host "--- 生成されたパス ---"
Write-Host $finalPath
